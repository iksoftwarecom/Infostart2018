
Процедура ОбработкаЗавершениеРаботы(ДопПараметры) Экспорт
	БылаОшибка = Неопределено;
	Если ТипЗнч(ДопПараметры) = Тип("Структура") Тогда
		ДопПараметры.Свойство("БылаОшибка",БылаОшибка);
	КонецЕсли;
	Если БылаОшибка <> Истина Тогда
		БылаОшибка = Истина;
	КонецЕсли;
	ЗавершитьРаботуСистемы(не БылаОшибка);
КонецПроцедуры
